# 🚀 Автоматическая инициализация базы данных на Vercel

## ✅ Что настроено

1. **Автоматические миграции** - выполняются при каждом деплое через `vercel-build`
2. **API endpoint для инициализации** - `/api/init-database` для создания админа и базовых данных

## 🔧 Как это работает

### При деплое на Vercel:

1. `npm install` - устанавливаются зависимости
2. `prisma generate` - генерируется Prisma Client
3. `prisma migrate deploy` - применяются миграции (создаются таблицы)
4. `next build` - собирается приложение

### После первого деплоя:

Вызовите API endpoint для инициализации данных:

```bash
curl -X POST https://your-domain.vercel.app/api/init-database \
  -H "Authorization: Bearer init-secret-key-change-in-production"
```

Или через браузер (если убрать проверку авторизации):

```
https://your-domain.vercel.app/api/init-database
```

## 🔐 Безопасность

По умолчанию endpoint защищен секретным ключом. Для продакшена:

1. Добавьте в Vercel Environment Variables:
   - **Key**: `INIT_DB_SECRET`
   - **Value**: ваш секретный ключ (минимум 32 символа)

2. Вызовите endpoint с ключом:
```bash
curl -X POST https://your-domain.vercel.app/api/init-database \
  -H "Authorization: Bearer ваш-секретный-ключ"
```

## 📋 Что создается при инициализации

- ✅ Администратор: `admin10@gmail.com` / `datmuf-Bajjyk-6wupde`
- ✅ Сети: TRC20, BEP20, ERC20, POLYGON
- ✅ Сетевые пары: 3 основные пары

## ⚠️ Важно

- Endpoint можно вызвать только один раз (проверка на существующего админа)
- После инициализации измените `INIT_DB_SECRET` на более надежный
- Или удалите endpoint после первого использования

