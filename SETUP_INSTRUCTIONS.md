# 🔧 Инструкция по исправлению проблем с npm install

## Проблема: ERR_SSL_CIPHER_OPERATION_FAILED

Если `npm install` падает с ошибкой SSL, попробуйте следующие решения:

## Решение 1: Отключить проверку SSL (временно)

```bash
npm config set strict-ssl false
npm install
npm config set strict-ssl true
```

## Решение 2: Очистить кеш и переустановить

```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

## Решение 3: Использовать другой registry

```bash
npm install --registry https://registry.npmjs.org/
```

## Решение 4: Использовать yarn

```bash
npm install -g yarn
yarn install
```

## Решение 5: Установить зависимости по частям

```bash
npm install --legacy-peer-deps
npx prisma generate
```

## После успешной установки

```bash
# 1. Создать .env.local с данными базы (см. SETUP_DATABASE.md)

# 2. Применить миграции
npx prisma migrate deploy

# 3. Инициализировать данные
npm run init-db

# 4. Инициализировать сетевые пары
npm run init-network-pairs
```

## ⚠️ Важно

- Файл `.env.local` должен содержать правильный `DATABASE_URL` для Neon
- После установки зависимостей обязательно выполните `npx prisma generate`
- Если проблемы продолжаются, попробуйте использовать Node.js версии 18 или 20 вместо 22

