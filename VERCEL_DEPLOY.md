# 🚀 Деплой на Vercel

## ✅ Готовность проекта

Проект успешно прошел проверку готовности:
- ✅ Сборка проходит без ошибок (`npm run build`)
- ✅ Все TypeScript ошибки исправлены
- ✅ Реферальная система полностью удалена
- ✅ Код выложен на GitHub: https://github.com/gizmofsc-cyber/dzenpay.git

## 🔧 Настройка Vercel

### 1. Подключение к GitHub
1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите репозиторий `gizmofsc-cyber/dzenpay`

### 2. Настройка переменных окружения

В настройках проекта Vercel добавьте следующие переменные:

```env
# База данных (PostgreSQL для продакшена)
DATABASE_URL="postgresql://username:password@host:port/database"

# NextAuth.js
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="your-production-secret-key-here"

# Окружение
NODE_ENV="production"
```

### 3. Настройка базы данных

#### Вариант A: Vercel Postgres (рекомендуется)
1. В панели Vercel перейдите в раздел "Storage"
2. Создайте новую PostgreSQL базу данных
3. Скопируйте `DATABASE_URL` из настроек базы
4. Добавьте эту переменную в Environment Variables

#### Вариант B: Внешняя PostgreSQL база
1. Создайте PostgreSQL базу на любом хостинге (Railway, Supabase, Neon)
2. Получите строку подключения
3. Добавьте в Environment Variables

### 4. Миграции базы данных

После создания базы данных выполните миграции:

```bash
# Локально (если нужно)
npx prisma migrate deploy

# Или через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy
```

### 5. Инициализация данных

После деплоя выполните инициализацию:

```bash
# Создание админа и базовых данных
npm run init-db

# Инициализация сетевых пар
npm run init-network-pairs
```

## 🎯 Особенности деплоя

### Build Command
Vercel автоматически использует команду из `package.json`:
```json
"build": "prisma generate && next build"
```

### Install Command
```json
"postinstall": "prisma generate"
```

### Конфигурация Vercel
Файл `vercel.json` уже настроен:
```json
{
  "buildCommand": "npm run build",
  "installCommand": "npm install && npx prisma generate",
  "env": {
    "NODE_ENV": "production"
  }
}
```

## 🔐 Безопасность

### Обязательные переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `NEXTAUTH_SECRET` - секретный ключ для сессий (минимум 32 символа)
- `NEXTAUTH_URL` - URL вашего домена

### Рекомендации:
- Используйте сильные пароли для базы данных
- Регулярно обновляйте `NEXTAUTH_SECRET`
- Настройте HTTPS (автоматически в Vercel)

## 📱 Проверка работы

После деплоя проверьте:

1. **Главная страница**: https://your-domain.vercel.app
2. **Регистрация**: Создайте тестового пользователя
3. **Админ панель**: Войдите как админ
4. **API endpoints**: Проверьте работу API

### Тестовые данные:
- **Админ**: admin10@gmail.com / datmuf-Bajjyk-6wupde

## 🚨 Возможные проблемы

### 1. Ошибки базы данных
- Проверьте `DATABASE_URL`
- Убедитесь что миграции применены
- Проверьте права доступа к базе

### 2. Ошибки сборки
- Проверьте логи в Vercel Dashboard
- Убедитесь что все зависимости установлены
- Проверьте TypeScript ошибки

### 3. Проблемы с сессиями
- Проверьте `NEXTAUTH_SECRET`
- Убедитесь что `NEXTAUTH_URL` правильный
- Проверьте настройки cookies

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в Vercel Dashboard
2. Проверьте переменные окружения
3. Обратитесь к менеджеру: [@alltimez](https://t.me/alltimez)

---

**Проект готов к деплою! 🎉**
