# 🚀 Инструкция по отправке проекта на GitHub

## Шаги для отправки проекта в репозиторий

Выполните следующие команды в терминале (Git Bash, CMD или PowerShell):

### 1. Проверьте, инициализирован ли git репозиторий

```bash
git status
```

Если репозиторий не инициализирован, выполните:

```bash
git init
```

### 2. Проверьте текущие remote репозитории

```bash
git remote -v
```

Если есть старый origin, удалите его:

```bash
git remote remove origin
```

### 3. Добавьте новый remote репозиторий

```bash
git remote add origin https://github.com/gizmofsc-cyber/dzenpay1.git
```

### 4. Добавьте все файлы в staging

```bash
git add .
```

### 5. Создайте коммит

```bash
git commit -m "Initial commit: Updated admin credentials to admin10@gmail.com"
```

### 6. Установите основную ветку (если нужно)

```bash
git branch -M main
```

### 7. Отправьте проект на GitHub

```bash
git push -u origin main
```

Если репозиторий на GitHub не пустой, используйте:

```bash
git push -u origin main --force
```

## ⚠️ Важно

- Убедитесь, что у вас есть права на запись в репозиторий `gizmofsc-cyber/dzenpay1`
- Если потребуется аутентификация, GitHub может запросить токен доступа или учетные данные
- Файл `.env.local` и другие чувствительные данные уже в `.gitignore` и не будут отправлены

## ✅ После успешной отправки

Проект будет доступен по адресу: https://github.com/gizmofsc-cyber/dzenpay1

## 🔧 Альтернативный способ (через GitHub Desktop)

1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку проекта
4. Нажмите "Publish repository"
5. Выберите `gizmofsc-cyber/dzenpay1` как репозиторий

